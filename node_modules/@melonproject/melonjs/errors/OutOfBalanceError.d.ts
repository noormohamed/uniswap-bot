import { ValidationError } from './ValidationError';
import BigNumber from 'bignumber.js';
export declare class OutOfBalanceError extends ValidationError {
    readonly amount: BigNumber;
    readonly balance: BigNumber;
    name: string;
    constructor(amount: BigNumber, balance: BigNumber, message?: string);
}
