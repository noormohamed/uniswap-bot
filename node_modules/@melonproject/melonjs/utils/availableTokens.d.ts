import { DeploymentOutput } from '../Deployment';
export interface TokenDefinition {
    symbol: string;
    name: string;
    address: string;
    decimals: number;
    historic: boolean;
}
export declare function availableTokens(deployment: DeploymentOutput): TokenDefinition[];
