import { Eth } from 'web3-eth';
import { Address } from '../../Address';
import { Environment, EnvironmentOptions } from '../../Environment';
interface TestEnvironmentOptions extends EnvironmentOptions {
    accounts: Address[];
}
export declare class TestEnvironment extends Environment {
    readonly accounts: Address[];
    readonly gasPrice: string;
    constructor(client: Eth, options: TestEnvironmentOptions);
}
export declare function createTestEnvironment(options?: EnvironmentOptions): Promise<TestEnvironment>;
export {};
