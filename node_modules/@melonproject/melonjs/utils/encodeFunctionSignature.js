"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const web3_eth_abi_1 = require("web3-eth-abi");
function encodeFunctionSignature(abi, name) {
    const abiItem = abi.find((item) => item.name === name);
    const abiCoder = new web3_eth_abi_1.AbiCoder();
    return abiCoder.encodeFunctionSignature(abiItem);
}
exports.encodeFunctionSignature = encodeFunctionSignature;
