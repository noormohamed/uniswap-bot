"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const Trading_errors_1 = require("../Trading.errors");
const ERC20WithFields_1 = require("../../../dependencies/token/ERC20WithFields");
exports.checkSufficientBalance = (environment, tokenAddress, tokenQuantity, vaultAddress) => __awaiter(void 0, void 0, void 0, function* () {
    const token = new ERC20WithFields_1.ERC20WithFields(environment, tokenAddress);
    const vaultTokenBalance = yield token.getBalanceOf(vaultAddress);
    if (vaultTokenBalance.isLessThan(tokenQuantity)) {
        throw new Trading_errors_1.InsufficientBalanceError(tokenQuantity, vaultTokenBalance);
    }
});
