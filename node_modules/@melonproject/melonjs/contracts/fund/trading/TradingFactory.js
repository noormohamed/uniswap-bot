"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const Contract_1 = require("../../../Contract");
const applyMixins_1 = require("../../../utils/applyMixins");
const Factory_1 = require("../../factory/Factory");
const TradingFactory_abi_1 = require("../../../abis/TradingFactory.abi");
class TradingFactory extends Contract_1.Contract {
    static deploy(environment, bytecode, from) {
        return super.createDeployment(environment, bytecode, from);
    }
    /**
     * Creates a Trading instance.
     *
     * @param from The sender address.
     * @param args The trading deploy arguments as [[TradingDeployArguments]].
     */
    createInstance(from, deployArgs) {
        const args = [deployArgs.hub, deployArgs.exchanges, deployArgs.adapters, deployArgs.registry];
        return this.createTransaction({ from, method: 'createInstance', args });
    }
}
exports.TradingFactory = TradingFactory;
TradingFactory.abi = TradingFactory_abi_1.TradingFactoryAbi;
applyMixins_1.applyMixins(TradingFactory, [Factory_1.Factory]);
