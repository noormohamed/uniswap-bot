"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const Contract_1 = require("../../../Contract");
const applyMixins_1 = require("../../../utils/applyMixins");
const Factory_1 = require("../../factory/Factory");
const ParticipationFactory_abi_1 = require("../../../abis/ParticipationFactory.abi");
class ParticipationFactory extends Contract_1.Contract {
    static deploy(environment, bytecode, from) {
        return super.createDeployment(environment, bytecode, from);
    }
    /**
     * Creates a ParticipationFactory instance.
     *
     * @param from The sender address.
     * @param args The participation deploy arguments as [[ParticipationDeployArguments]].
     */
    createInstance(from, deployArgs) {
        const args = [deployArgs.hub, deployArgs.defaultAssets, deployArgs.registry];
        return this.createTransaction({ from, method: 'createInstance', args });
    }
}
exports.ParticipationFactory = ParticipationFactory;
ParticipationFactory.abi = ParticipationFactory_abi_1.ParticipationFactoryAbi;
applyMixins_1.applyMixins(ParticipationFactory, [Factory_1.Factory]);
