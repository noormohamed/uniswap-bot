"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const createTestEnvironment_1 = require("../../../utils/tests/createTestEnvironment");
const randomAddress_1 = require("../../../utils/tests/randomAddress");
const deployHub_1 = require("../../../utils/tests/deployHub");
const VaultFactory_1 = require("./VaultFactory");
const VaultFactory_bin_1 = require("../../../abis/VaultFactory.bin");
describe('VaultFactory', () => {
    let environment;
    let vaultFactory;
    beforeAll(() => __awaiter(void 0, void 0, void 0, function* () {
        environment = yield createTestEnvironment_1.createTestEnvironment();
        const deploy = VaultFactory_1.VaultFactory.deploy(environment, VaultFactory_bin_1.VaultFactoryBytecode, environment.accounts[0]);
        vaultFactory = yield deploy.send(yield deploy.prepare());
    }));
    it('should check if a contract is an instance of the VaultFactory', () => __awaiter(void 0, void 0, void 0, function* () {
        const result = yield vaultFactory.isInstance(randomAddress_1.randomAddress());
        expect(typeof result).toBe('boolean');
    }));
    it('should create an instance of a Vault contract', () => __awaiter(void 0, void 0, void 0, function* () {
        const hub = yield deployHub_1.deployHub(environment, environment.accounts[0], {
            name: 'vault-test-fund',
            manager: environment.accounts[0],
        });
        const tx = vaultFactory.createInstance(environment.accounts[0], hub.contract.address);
        const txResult = yield tx.send(yield tx.prepare());
        expect(txResult.gasUsed).toBeGreaterThanOrEqual(0);
        expect(txResult.status).toBe(true);
    }));
});
