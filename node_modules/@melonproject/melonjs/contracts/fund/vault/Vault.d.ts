import { Contract } from '../../../Contract';
import { Environment } from '../../../Environment';
import { Address } from '../../../Address';
import { Spoke } from '../hub/Spoke';
import BigNumber from 'bignumber.js';
export declare class Vault extends Contract {
    static readonly abi: import("web3-utils").AbiItem[];
    static deploy(environment: Environment, bytecode: string, from: Address, hub: Address): import("../../..").Deployment<Vault>;
    /**
     * Withdraw from Vault
     *
     * @param from The sender address.
     * @param token The address of the token to withdraw
     * @param amount The amount to withdraw
     */
    withdraw(from: Address, token: Address, amount: BigNumber): import("../../..").Transaction<import("web3-core").TransactionReceipt>;
}
export interface Vault extends Spoke {
}
