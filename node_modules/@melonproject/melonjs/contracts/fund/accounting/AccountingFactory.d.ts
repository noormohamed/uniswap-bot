import { Contract } from '../../../Contract';
import { Environment } from '../../../Environment';
import { Address } from '../../../Address';
import { Factory } from '../../factory/Factory';
import { AccountingDeployArguments } from './Accounting';
export declare class AccountingFactory extends Contract {
    static readonly abi: import("web3-utils").AbiItem[];
    static deploy(environment: Environment, bytecode: string, from: Address): import("../../..").Deployment<AccountingFactory>;
    /**
     * Creates an Accounting instance.
     *
     * @param from The sender address.
     * @param args The accounting deploy arguments as [[AccountingDeployArguments]].
     */
    createInstance(from: Address, deployArgs: AccountingDeployArguments): import("../../..").Transaction<import("web3-core").TransactionReceipt>;
}
export interface AccountingFactory extends Factory {
}
