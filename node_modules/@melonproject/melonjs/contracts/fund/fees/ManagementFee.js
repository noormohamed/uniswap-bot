"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const IFee_1 = require("./IFee");
const ManagementFee_abi_1 = require("../../../abis/ManagementFee.abi");
const toBigNumber_1 = require("../../../utils/toBigNumber");
class ManagementFee extends IFee_1.IFee {
    static deploy(environment, bytecode, from) {
        return super.createDeployment(environment, bytecode, from);
    }
    /**
     * Gets the management fee rate.
     *
     * @param feeManagerAddress The address of the fee manager contract
     * @param block The block number to execute the call on.
     */
    getManagementFeeRate(feeManagerAddress, block) {
        return __awaiter(this, void 0, void 0, function* () {
            const result = yield this.makeCall('managementFeeRate', [feeManagerAddress], block);
            return toBigNumber_1.toBigNumber(result);
        });
    }
}
exports.ManagementFee = ManagementFee;
ManagementFee.abi = ManagementFee_abi_1.ManagementFeeAbi;
