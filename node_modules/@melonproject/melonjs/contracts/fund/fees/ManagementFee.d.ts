import { IFee } from './IFee';
import { Environment } from '../../../Environment';
import { Address } from '../../../Address';
export declare class ManagementFee extends IFee {
    static readonly abi: import("web3-utils").AbiItem[];
    static deploy(environment: Environment, bytecode: string, from: Address): import("../../..").Deployment<ManagementFee>;
    /**
     * Gets the management fee rate.
     *
     * @param feeManagerAddress The address of the fee manager contract
     * @param block The block number to execute the call on.
     */
    getManagementFeeRate(feeManagerAddress: Address, block?: number): Promise<import("bignumber.js").default>;
}
