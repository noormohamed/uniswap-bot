"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const PriceTolerance_1 = require("./PriceTolerance");
const createTestEnvironment_1 = require("../../../utils/tests/createTestEnvironment");
const PriceTolerance_bin_1 = require("../../../abis/PriceTolerance.bin");
const bignumber_js_1 = __importDefault(require("bignumber.js"));
describe('PriceTolerance', () => {
    let environment;
    let priceTolerance;
    const tolerance = 10;
    beforeAll(() => __awaiter(void 0, void 0, void 0, function* () {
        environment = yield createTestEnvironment_1.createTestEnvironment();
        const deploy = PriceTolerance_1.PriceTolerance.deploy(environment, PriceTolerance_bin_1.PriceToleranceBytecode, environment.accounts[0], tolerance);
        priceTolerance = yield deploy.send(yield deploy.prepare());
    }));
    it('should return the price tolerance', () => __awaiter(void 0, void 0, void 0, function* () {
        const result = yield priceTolerance.getPriceTolerance();
        expect(result.isEqualTo(new bignumber_js_1.default('1e16').multipliedBy(tolerance))).toBe(true);
    }));
});
