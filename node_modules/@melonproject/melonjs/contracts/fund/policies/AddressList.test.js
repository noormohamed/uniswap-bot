"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const AddressList_1 = require("./AddressList");
const createTestEnvironment_1 = require("../../../utils/tests/createTestEnvironment");
const AddressList_bin_1 = require("../../../abis/AddressList.bin");
const randomAddress_1 = require("../../../utils/tests/randomAddress");
const sameAddress_1 = require("../../../utils/sameAddress");
const range_1 = require("../../../utils/range");
describe('AddressList', () => {
    let environment;
    let addressList;
    const addresses = range_1.range(5).map(() => randomAddress_1.randomAddress());
    beforeAll(() => __awaiter(void 0, void 0, void 0, function* () {
        environment = yield createTestEnvironment_1.createTestEnvironment();
        const deploy = AddressList_1.AddressList.deploy(environment, AddressList_bin_1.AddressListBytecode, environment.accounts[0], addresses);
        addressList = yield deploy.send(yield deploy.prepare());
    }));
    it('should check if an address is member of an address list', () => __awaiter(void 0, void 0, void 0, function* () {
        const result = yield addressList.isMember(addresses[1]);
        expect(result === true || result === false).toBe(true);
    }));
    it('should return the correct number of members', () => __awaiter(void 0, void 0, void 0, function* () {
        const result = yield addressList.getMemberCount();
        expect(result.isEqualTo(addresses.length)).toBe(true);
    }));
    it('should return a list of members', () => __awaiter(void 0, void 0, void 0, function* () {
        const result = yield addressList.getMembers();
        result.map((address) => {
            expect(addresses.some((a) => sameAddress_1.sameAddress(a, address))).toBe(true);
        });
    }));
});
