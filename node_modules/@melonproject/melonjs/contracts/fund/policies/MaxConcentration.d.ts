import { IPolicy } from './IPolicy';
import { Environment } from '../../../Environment';
import { Address } from '../../../Address';
import BigNumber from 'bignumber.js';
export declare class MaxConcentration extends IPolicy {
    static readonly abi: import("web3-utils").AbiItem[];
    static deploy(environment: Environment, bytecode: string, from: Address, maxConcentration: BigNumber): import("../../..").Deployment<MaxConcentration>;
    /**
     * Gets the maximum concentration of an asset in the portfolio.
     *
     * @param block The block number to execute the call on.
     */
    getMaxConcentration(block?: number): Promise<BigNumber>;
}
