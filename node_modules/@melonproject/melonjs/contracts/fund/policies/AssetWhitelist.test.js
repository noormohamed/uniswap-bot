"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const AssetWhitelist_1 = require("./AssetWhitelist");
const createTestEnvironment_1 = require("../../../utils/tests/createTestEnvironment");
const randomAddress_1 = require("../../../utils/tests/randomAddress");
const AssetWhitelist_bin_1 = require("../../../abis/AssetWhitelist.bin");
const range_1 = require("../../../utils/range");
describe('AssetWhitelist', () => {
    let environment;
    let assetWhitelist;
    const addresses = range_1.range(5).map(() => randomAddress_1.randomAddress());
    beforeAll(() => __awaiter(void 0, void 0, void 0, function* () {
        environment = yield createTestEnvironment_1.createTestEnvironment();
        const deploy = AssetWhitelist_1.AssetWhitelist.deploy(environment, AssetWhitelist_bin_1.AssetWhitelistBytecode, environment.accounts[0], addresses);
        assetWhitelist = yield deploy.send(yield deploy.prepare());
    }));
    it('should return the correct position', () => __awaiter(void 0, void 0, void 0, function* () {
        const result = yield assetWhitelist.getPosition();
        expect(result).toBe(0);
    }));
    it('should return the correct identifier', () => __awaiter(void 0, void 0, void 0, function* () {
        const result = yield assetWhitelist.getIdentifier();
        expect(result).toBe('AssetWhitelist');
    }));
});
