import { Contract } from '../../../Contract';
import { IPolicy } from './IPolicy';
import { AddressList } from './AddressList';
import { Environment } from '../../../Environment';
import { Address } from '../../../Address';
export declare class AssetBlacklist extends Contract {
    static readonly abi: import("web3-utils").AbiItem[];
    static deploy(environment: Environment, bytecode: string, from: Address, members: Address[]): import("../../..").Deployment<AssetBlacklist>;
    /**
     * Adds a list of assets to an AssetBlacklist.
     *
     * @param from The address of the sender
     * @param asset The address of the asset to be added to the AssetBlacklist
     */
    addToBlacklist(from: Address, asset: Address): import("../../..").Transaction<import("web3-core").TransactionReceipt>;
}
export interface AssetBlacklist extends IPolicy, AddressList {
}
