import { IPolicy } from './IPolicy';
import { Environment } from '../../../Environment';
import { Address } from '../../../Address';
export declare class PriceTolerance extends IPolicy {
    static readonly abi: import("web3-utils").AbiItem[];
    static deploy(environment: Environment, bytecode: string, from: Address, tolerance: number): import("../../..").Deployment<PriceTolerance>;
    /**
     * Gets the price tolerance
     *
     * @param block The block number to execute the call on.
     */
    getPriceTolerance(block?: number): Promise<import("bignumber.js").default>;
}
