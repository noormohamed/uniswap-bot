"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const Contract_1 = require("../../../Contract");
const applyMixins_1 = require("../../../utils/applyMixins");
const Factory_1 = require("../../factory/Factory");
const PolicyManagerFactory_abi_1 = require("../../../abis/PolicyManagerFactory.abi");
class PolicyManagerFactory extends Contract_1.Contract {
    static deploy(environment, bytecode, from) {
        return super.createDeployment(environment, bytecode, from);
    }
    /**
     * Creates a PolicyManager instance.
     *
     * @param from The sender address.
     * @param hub The hub address.
     */
    createInstance(from, hub) {
        const args = [hub];
        return this.createTransaction({ from, method: 'createInstance', args });
    }
}
exports.PolicyManagerFactory = PolicyManagerFactory;
PolicyManagerFactory.abi = PolicyManagerFactory_abi_1.PolicyManagerFactoryAbi;
applyMixins_1.applyMixins(PolicyManagerFactory, [Factory_1.Factory]);
