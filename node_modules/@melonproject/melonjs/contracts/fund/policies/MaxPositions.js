"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const MaxPositions_abi_1 = require("../../../abis/MaxPositions.abi");
const IPolicy_1 = require("./IPolicy");
const web3_utils_1 = require("web3-utils");
class MaxPositions extends IPolicy_1.IPolicy {
    static deploy(environment, bytecode, from, maxPositions) {
        return super.createDeployment(environment, bytecode, from, [maxPositions]);
    }
    /**
     * Gets the maximum number of positions.
     *
     * @param block The block number to execute the call on.
     */
    getMaxPositions(block) {
        return __awaiter(this, void 0, void 0, function* () {
            const result = yield this.makeCall('maxPositions', undefined, block);
            return web3_utils_1.hexToNumber(result);
        });
    }
}
exports.MaxPositions = MaxPositions;
MaxPositions.abi = MaxPositions_abi_1.MaxPositionsAbi;
