"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const PriceTolerance_abi_1 = require("../../../abis/PriceTolerance.abi");
const toBigNumber_1 = require("../../../utils/toBigNumber");
const IPolicy_1 = require("./IPolicy");
class PriceTolerance extends IPolicy_1.IPolicy {
    static deploy(environment, bytecode, from, tolerance) {
        return super.createDeployment(environment, bytecode, from, [tolerance]);
    }
    /**
     * Gets the price tolerance
     *
     * @param block The block number to execute the call on.
     */
    getPriceTolerance(block) {
        return __awaiter(this, void 0, void 0, function* () {
            const result = yield this.makeCall('tolerance', undefined, block);
            return toBigNumber_1.toBigNumber(result);
        });
    }
}
exports.PriceTolerance = PriceTolerance;
PriceTolerance.abi = PriceTolerance_abi_1.PriceToleranceAbi;
