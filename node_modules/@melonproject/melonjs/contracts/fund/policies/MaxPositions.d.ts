import { IPolicy } from './IPolicy';
import { Environment } from '../../../Environment';
import { Address } from '../../../Address';
export declare class MaxPositions extends IPolicy {
    static readonly abi: import("web3-utils").AbiItem[];
    static deploy(environment: Environment, bytecode: string, from: Address, maxPositions: number): import("../../..").Deployment<MaxPositions>;
    /**
     * Gets the maximum number of positions.
     *
     * @param block The block number to execute the call on.
     */
    getMaxPositions(block?: number): Promise<number>;
}
