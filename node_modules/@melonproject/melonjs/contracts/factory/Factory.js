"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const Contract_1 = require("../../Contract");
const Factory_abi_1 = require("../../abis/Factory.abi");
class Factory extends Contract_1.Contract {
    static deploy(environment, bytecode, from) {
        return super.createDeployment(environment, bytecode, from);
    }
    /**
     * Checks if an address is an instance of the factory
     *
     * @param address The address of the contract to check
     * @param block The block number to execute the call on.
     */
    isInstance(address, block) {
        return this.makeCall('isInstance', [address], block);
    }
}
exports.Factory = Factory;
Factory.abi = Factory_abi_1.FactoryAbi;
