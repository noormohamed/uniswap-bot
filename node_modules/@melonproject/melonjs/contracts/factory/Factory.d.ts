import { Contract } from '../../Contract';
import { Address } from '../../Address';
import { Environment } from '../../Environment';
export declare class Factory extends Contract {
    static readonly abi: import("web3-utils").AbiItem[];
    static deploy(environment: Environment, bytecode: string, from: Address): import("../..").Deployment<Factory>;
    /**
     * Checks if an address is an instance of the factory
     *
     * @param address The address of the contract to check
     * @param block The block number to execute the call on.
     */
    isInstance(address: Address, block?: number): Promise<boolean>;
}
