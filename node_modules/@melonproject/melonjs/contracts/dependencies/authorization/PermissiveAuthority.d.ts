import { Environment } from '../../../Environment';
import { Address } from '../../../Address';
import { Contract } from '../../../Contract';
import { DSAuthority } from './DSAuthority';
export declare class PermissiveAuthority extends Contract {
    static readonly abi: import("web3-utils").AbiItem[];
    static deploy(environment: Environment, bytecode: string, from: Address): import("../../..").Deployment<PermissiveAuthority>;
}
export interface PermissiveAuthority extends DSAuthority {
}
