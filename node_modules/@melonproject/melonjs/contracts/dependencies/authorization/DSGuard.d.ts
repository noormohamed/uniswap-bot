import { Environment } from '../../../Environment';
import { Address } from '../../../Address';
import { Contract } from '../../../Contract';
import { DSAuth } from './DSAuth';
import { DSAuthority } from './DSAuthority';
export declare class DSGuard extends Contract {
    static readonly abi: import("web3-utils").AbiItem[];
    static deploy(environment: Environment, bytecode: string, from: Address): import("../../..").Deployment<DSGuard>;
}
export interface DSGuard extends DSAuth, DSAuthority {
}
