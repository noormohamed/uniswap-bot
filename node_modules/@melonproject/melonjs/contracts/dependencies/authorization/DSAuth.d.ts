import { Environment } from '../../../Environment';
import { Address } from '../../../Address';
import { Contract } from '../../../Contract';
export declare class DSAuth extends Contract {
    static readonly abi: import("web3-utils").AbiItem[];
    static deploy(environment: Environment, bytecode: string, from: Address): import("../../..").Deployment<DSAuth>;
    /**
     * Gets the address of the owner
     *
     * @param block The block number to execute the call on.
     */
    getOwner(block?: number): Promise<string>;
}
