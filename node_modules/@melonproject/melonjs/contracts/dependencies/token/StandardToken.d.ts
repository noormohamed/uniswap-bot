import { Environment } from '../../../Environment';
import { Address } from '../../../Address';
import { Contract } from '../../../Contract';
import BigNumber from 'bignumber.js';
import { ERC20WithFields } from './ERC20WithFields';
export declare class StandardToken extends Contract {
    static readonly abi: import("web3-utils").AbiItem[];
    static deploy(environment: Environment, bytecode: string, from: Address): import("../../..").Deployment<StandardToken>;
    /**
     * Gets the total supply of the token.
     *
     * @param block The block number to execute the call on.
     */
    getTotalSupply(block?: number): Promise<BigNumber>;
    /**
     * Gets the balance of the specified address.
     *
     * @param owner The address of the owner
     * @param block The block number to execute the call on.
     */
    getBalanceOf(owner: Address, block?: number): Promise<BigNumber>;
    /**
     * Gets the amount of tokens that an owner allowed to a spender.
     *
     * @param owner The address of the owner
     * @param spender The address of the spender
     * @param block The block number to execute the call on.
     */
    getAllowance(owner: Address, spender: Address, block?: number): Promise<BigNumber>;
    /**
     * Transfer tokens to a specified address
     *
     * @param from The address to transfer from
     * @param to The address to transfer to
     * @param amount The amount to transfer
     */
    transfer(from: Address, to: Address, amount: BigNumber): import("../../..").Transaction<import("web3-core").TransactionReceipt>;
    /**
     * Approve the spender address to spend the specified amount of tokens on behalf of the owner address
     *
     * @param from The address to transfer from
     * @param spender The address to transfer to
     * @param amount The amount to transfer
     */
    approve(from: Address, spender: Address, amount: BigNumber): import("../../..").Transaction<import("web3-core").TransactionReceipt>;
    /**
     * Increase the approved amount of tokens that a spender can spend
     *
     * @param from The address to transfer from
     * @param spender The address to transfer to
     * @param amount The amount to transfer
     */
    increaseApproval(from: Address, spender: Address, amount: BigNumber): import("../../..").Transaction<import("web3-core").TransactionReceipt>;
    /**
     * Decrease the approved amount of tokens that a spender can spend
     *
     * @param from The address to transfer from
     * @param spender The address to transfer to
     * @param amount The amount to transfer
     */
    decreaseApproval(from: Address, spender: Address, amount: BigNumber): import("../../..").Transaction<import("web3-core").TransactionReceipt>;
}
export interface StandardToken extends ERC20WithFields {
}
