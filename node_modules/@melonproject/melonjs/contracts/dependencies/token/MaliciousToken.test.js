"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const createTestEnvironment_1 = require("../../../utils/tests/createTestEnvironment");
const PreminedToken_1 = require("./PreminedToken");
const MaliciousToken_bin_1 = require("../../../abis/MaliciousToken.bin");
describe('MaliciousToken', () => {
    let environment;
    let maliciousToken;
    beforeAll(() => __awaiter(void 0, void 0, void 0, function* () {
        environment = yield createTestEnvironment_1.createTestEnvironment();
        const deploy = PreminedToken_1.PreminedToken.deploy(environment, MaliciousToken_bin_1.MaliciousTokenBytecode, environment.accounts[0], {
            symbol: 'MMM',
            decimals: 15,
            name: 'Malicous Monster Money',
        });
        maliciousToken = yield deploy.send(yield deploy.prepare());
    }));
    it('should return balance of the token', () => __awaiter(void 0, void 0, void 0, function* () {
        const result = yield maliciousToken.getTotalSupply();
        expect(result.isGreaterThanOrEqualTo(0)).toBe(true);
    }));
    it('should return the symbol of the token', () => __awaiter(void 0, void 0, void 0, function* () {
        const result = yield maliciousToken.getSymbol();
        expect(result).toBe('MMM');
    }));
});
