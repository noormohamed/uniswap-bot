import { Environment } from '../../../Environment';
import { Address } from '../../../Address';
import { Contract } from '../../../Contract';
import { PreminedToken } from './PreminedToken';
export interface PreminedTokenDeployArguments {
    symbol: string;
    decimals: number;
    name: string;
}
export declare class MaliciousToken extends Contract {
    static readonly abi: import("web3-utils").AbiItem[];
    static deploy(environment: Environment, bytecode: string, from: Address, args: PreminedTokenDeployArguments): import("../../..").Deployment<MaliciousToken>;
}
export interface MaliciousToken extends PreminedToken {
}
