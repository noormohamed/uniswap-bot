import { Environment } from '../../../Environment';
import { Address } from '../../../Address';
import { Contract } from '../../../Contract';
import { PreminedToken, PreminedTokenDeployArguments } from './PreminedToken';
import { BigNumber } from 'bignumber.js';
export declare class BurnableToken extends Contract {
    static readonly abi: import("web3-utils").AbiItem[];
    static deploy(environment: Environment, bytecode: string, from: Address, args: PreminedTokenDeployArguments): import("../../..").Deployment<BurnableToken>;
    /**
     * Burn tokens
     *
     * @param from The address to burn
     * @param amount The amount to transfer
     */
    burn(from: Address, amount: BigNumber): import("../../..").Transaction<import("web3-core").TransactionReceipt>;
}
export interface BurnableToken extends PreminedToken {
}
