"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const Contract_1 = require("../../../Contract");
const applyMixins_1 = require("../../../utils/applyMixins");
const PreminedToken_1 = require("./PreminedToken");
const BurnableToken_abi_1 = require("../../../abis/BurnableToken.abi");
const OutOfBalanceError_1 = require("../../../errors/OutOfBalanceError");
const isZeroAddress_1 = require("../../../utils/isZeroAddress");
const ZeroAddressError_1 = require("../../../errors/ZeroAddressError");
class BurnableToken extends Contract_1.Contract {
    static deploy(environment, bytecode, from, args) {
        return super.createDeployment(environment, bytecode, from, [args.symbol, args.decimals, args.name]);
    }
    /**
     * Burn tokens
     *
     * @param from The address to burn
     * @param amount The amount to transfer
     */
    burn(from, amount) {
        const args = [amount.toFixed(0)];
        const validate = () => __awaiter(this, void 0, void 0, function* () {
            if (isZeroAddress_1.isZeroAddress(from))
                throw new ZeroAddressError_1.ZeroAddressError();
            const balance = yield this.getBalanceOf(from);
            if (balance.isLessThan(amount)) {
                throw new OutOfBalanceError_1.OutOfBalanceError(amount, balance);
            }
        });
        return this.createTransaction({ from, method: 'burn', args, validate });
    }
}
exports.BurnableToken = BurnableToken;
BurnableToken.abi = BurnableToken_abi_1.BurnableTokenAbi;
applyMixins_1.applyMixins(BurnableToken, [PreminedToken_1.PreminedToken]);
