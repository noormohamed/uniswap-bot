import { Environment } from '../../../Environment';
import { Address } from '../../../Address';
import { Contract } from '../../../Contract';
import { StandardToken } from './StandardToken';
export interface PreminedTokenDeployArguments {
    symbol: string;
    decimals: number;
    name: string;
}
export declare class PreminedToken extends Contract {
    static readonly abi: import("web3-utils").AbiItem[];
    static deploy(environment: Environment, bytecode: string, from: Address, args: PreminedTokenDeployArguments): import("../../..").Deployment<PreminedToken>;
}
export interface PreminedToken extends StandardToken {
}
