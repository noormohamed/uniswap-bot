import { Contract } from '../../Contract';
import BigNumber from 'bignumber.js';
export declare class AmguConsumer extends Contract {
    static readonly abi: import("web3-utils").AbiItem[];
    /**
     * Gets the amgu token.
     *
     * @param block The block number to execute the call on.
     */
    getAmguToken(block?: number): Promise<string>;
    /**
     * Gets the engine address.
     *
     * @param block The block number to execute the call on.
     */
    getEngine(block?: number): Promise<string>;
    /**
     * Gets the price source address.
     *
     * @param block The block number to execute the call on.
     */
    getPriceSource(block?: number): Promise<string>;
    /**
     * Gets the version address.
     *
     * @param block The block number to execute the call on.
     */
    getVersion(block?: number): Promise<string>;
    /**
     * Calculates the required AMGU value for a transaction.
     *
     * @param gasEstimation The gas estimation value.
     * @param block The block number to execute the call on.
     */
    protected calculateAmgu(gasEstimation: number, block?: number): Promise<BigNumber>;
}
