import { IPriceSource } from './IPriceSource';
import { Address } from '../../Address';
import BigNumber from 'bignumber.js';
export interface PriceUpdate {
    asset: Address;
    price: BigNumber;
}
export declare class TestingPriceFeed extends IPriceSource {
    static readonly abi: import("web3-utils").AbiItem[];
    /**
     * Update the price feed.
     *
     * @param block The block number to execute the call on.
     */
    update(from: Address, updates: PriceUpdate[]): import("../..").Transaction<import("web3-core").TransactionReceipt>;
}
