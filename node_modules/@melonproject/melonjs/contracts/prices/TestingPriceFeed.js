"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const TestingPriceFeed_abi_1 = require("../../abis/TestingPriceFeed.abi");
const IPriceSource_1 = require("./IPriceSource");
class TestingPriceFeed extends IPriceSource_1.IPriceSource {
    /**
     * Update the price feed.
     *
     * @param block The block number to execute the call on.
     */
    update(from, updates) {
        const prices = updates.map((item) => item.price.toFixed(0));
        const assets = updates.map((item) => item.asset);
        return this.createTransaction({ from, method: 'update', args: [assets, prices] });
    }
}
exports.TestingPriceFeed = TestingPriceFeed;
TestingPriceFeed.abi = TestingPriceFeed_abi_1.TestingPriceFeedAbi;
