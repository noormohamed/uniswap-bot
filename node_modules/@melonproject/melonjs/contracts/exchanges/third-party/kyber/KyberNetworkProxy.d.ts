import BigNumber from 'bignumber.js';
import { Contract } from '../../../../Contract';
import { Environment } from '../../../../Environment';
import { Address } from '../../../../Address';
export interface KyberExpectedRate {
    expectedRate: BigNumber;
    slippageRate: BigNumber;
}
export declare class KyberNetworkProxy extends Contract {
    static readonly abi: import("web3-utils").AbiItem[];
    static deploy(environment: Environment, bytecode: string, from: Address): import("../../../..").Deployment<KyberNetworkProxy>;
    /**
     * Gets the expected rate for a given asset pair.
     *
     * @param srcToken The maker asset address.
     * @param destToken The taker asset address.
     * @param srcQty The maker asset amount.
     * @param block The block number to execute the call on.
     */
    getExpectedRate(srcToken: Address, destToken: Address, srcQty: BigNumber, block?: number): Promise<KyberExpectedRate>;
}
