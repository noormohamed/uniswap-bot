import { Contract } from '../../../../Contract';
import { Environment } from '../../../../Environment';
import { Address } from '../../../../Address';
export declare class UniswapFactory extends Contract {
    static readonly abi: import("web3-utils").AbiItem[];
    static deploy(environment: Environment, bytecode: string, from: Address): import("../../../..").Deployment<UniswapFactory>;
    /**
     * Gets the exchange address
     *
     * @param token The maker asset address.
     * @param block The block number to execute the call on.
     */
    getExchange(token: Address, block?: number): Promise<string>;
}
