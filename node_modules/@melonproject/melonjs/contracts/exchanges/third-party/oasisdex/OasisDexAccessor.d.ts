import { Contract } from '../../../../Contract';
import { Environment } from '../../../../Environment';
import { Address } from '../../../../Address';
import BigNumber from 'bignumber.js';
export interface OasisDexOrder {
    id: BigNumber;
    sellQuantity: BigNumber;
    buyQuantity: BigNumber;
}
export declare class OasisDexAccessor extends Contract {
    static readonly abi: import("web3-utils").AbiItem[];
    static deploy(environment: Environment, bytecode: string, from: Address): import("../../../..").Deployment<OasisDexAccessor>;
    /**
     * Gets current market orders for a given asset pair.
     *
     * @param targetExchange The address of the exchange.
     * @param sellAsset The sell asset address.
     * @param buyAsset The buy asset address.
     * @param block The block number to execute the call on.
     */
    getOrders(targetExchange: Address, sellAsset: Address, buyAsset: Address, block?: number): Promise<OasisDexOrder[]>;
}
